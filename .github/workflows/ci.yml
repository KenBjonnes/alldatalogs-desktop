name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      # The committed payload must match its manifest (the source folders live on Ken's PC, so CI
      # verifies internal consistency and script order; source-vs-payload runs at release time).
      - run: node scripts/build-payload.mjs --check
      - run: npm test
